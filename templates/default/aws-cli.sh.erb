# Generated by Chef for <%= node[:fqdn] %>

# bash autocompletion for aws cli
if [ "$BASH" ]; then
  complete -C aws_completer aws
fi

# zsh autocompletion for aws cli
if [ "$ZSH_VERSION" ]; then
  autoload -U compinit compdef
  compinit
  source /usr/local/bin/aws_zsh_completer.sh
fi
